# macOS / editors
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Temporary / logs
*.tmp
*.log

# Private originals: the compressed images ship with the site, the full-resolution
# shots and the local private album never enter the repo
/assets/img/originals/

# Internal design notes: contain private details and the "the entry gate is not real
# encryption" caveat, so they must not ship with a public repo (see docs/design.md §9)
/docs/

# Prototype output: inlines the real photos, kept out of the public repo
# (regenerate with prototype/build_prototype.py)
/prototype/prototype.html
